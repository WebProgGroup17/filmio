# Create your own .env file in the frontend folder and add the following variable for the API connection:

# VITE_API_URL=http://localhost:3001

# The frontend has its own .env because it only needs VITE_-prefixed variables and backend secrets should not be exposed to the browser
